{
  "name": "@diffract/api",
  "version": "0.1.0",
  "description": "Diffract REST API bridge — wraps OpenShell gRPC gateway for web consumption",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "@grpc/grpc-js": "^1.12.6",
    "@grpc/proto-loader": "^0.7.13",
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "express": "^5.1.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
