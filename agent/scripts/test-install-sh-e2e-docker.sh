#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
IMAGE_NAME="${DIFFRACTION_INSTALL_E2E_IMAGE:-diffraction-install-e2e:local}"
INSTALL_URL="${DIFFRACTION_INSTALL_URL:-https://diffraction.bot/install.sh}"

OPENAI_API_KEY="${OPENAI_API_KEY:-}"
ANTHROPIC_API_KEY="${ANTHROPIC_API_KEY:-}"
ANTHROPIC_API_TOKEN="${ANTHROPIC_API_TOKEN:-}"
DIFFRACTION_E2E_MODELS="${DIFFRACTION_E2E_MODELS:-}"

echo "==> Build image: $IMAGE_NAME"
docker build \
  -t "$IMAGE_NAME" \
  -f "$ROOT_DIR/scripts/docker/install-sh-e2e/Dockerfile" \
  "$ROOT_DIR/scripts/docker/install-sh-e2e"

echo "==> Run E2E installer test"
docker run --rm \
  -e DIFFRACTION_INSTALL_URL="$INSTALL_URL" \
  -e DIFFRACTION_INSTALL_TAG="${DIFFRACTION_INSTALL_TAG:-latest}" \
  -e DIFFRACTION_E2E_MODELS="$DIFFRACTION_E2E_MODELS" \
  -e DIFFRACTION_INSTALL_E2E_PREVIOUS="${DIFFRACTION_INSTALL_E2E_PREVIOUS:-}" \
  -e DIFFRACTION_INSTALL_E2E_SKIP_PREVIOUS="${DIFFRACTION_INSTALL_E2E_SKIP_PREVIOUS:-0}" \
  -e OPENAI_API_KEY="$OPENAI_API_KEY" \
  -e ANTHROPIC_API_KEY="$ANTHROPIC_API_KEY" \
  -e ANTHROPIC_API_TOKEN="$ANTHROPIC_API_TOKEN" \
  "$IMAGE_NAME"
