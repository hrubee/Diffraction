apiVersion: v1
kind: Service
metadata:
  name: diffraction
  labels:
    app: diffraction
spec:
  type: ClusterIP
  selector:
    app: diffraction
  ports:
    - name: gateway
      port: 18789
      targetPort: 18789
      protocol: TCP
