{
  "id": "diffraction",
  "name": "Diffraction",
  "version": "0.1.0",
  "description": "Migrate and run Diffraction inside OpenShell with optional NIM-backed inference",
  "configSchema": {
    "type": "object",
    "properties": {
      "blueprintVersion": {
        "type": "string",
        "description": "Pinned blueprint artifact version (e.g., '0.1.0'). Omit for latest.",
        "default": "latest"
      },
      "blueprintRegistry": {
        "type": "string",
        "description": "OCI registry or GitHub release URL for blueprint artifacts",
        "default": "ghcr.io/nvidia/diffraction-blueprint"
      },
      "sandboxName": {
        "type": "string",
        "description": "Name for the Diffraction sandbox in OpenShell",
        "default": "diffraction"
      },
      "inferenceProvider": {
        "type": "string",
        "description": "Default inference provider type (nvidia, vllm, openai-compatible)",
        "default": "nvidia"
      }
    },
    "additionalProperties": false
  }
}
